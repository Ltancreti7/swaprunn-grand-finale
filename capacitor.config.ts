const config = {
  appId: "com.swaprunn.app",
  appName: "SwapRunn",
  webDir: "dist",
  bundledWebRuntime: false,
  server: {
    url: "https://swaprunn.com/", //
    cleartext: true,
    allowNavigation: ["swaprunn.com"]
  }
};

export default config;
